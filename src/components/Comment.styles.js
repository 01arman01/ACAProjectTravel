import { createUseStyles } from "react-jss";

export const useCommentStyles = createUseStyles({
  deleteIcon: {
    position: "absolute",
    right: "17px",
    top: "21px",
    zIndex: "1000",
    cursor:"pointer"
  },
});
